# Sub2API 精简版：上游修复与核心能力更新工作流

## 1. 触发与目标

用户说“与上游项目同步修复与更新”“与主项目同步修复与更新”或类似指令时，按本工作流执行：先分析并提交候选清单，人工确认后实施，不解释为全量同步。

- 上游：<https://github.com/Wei-Shaw/sub2api>
- 精简版及最终 PR 目标：<https://github.com/CherryLover/sub2api>
- 个人中转仓库：<https://github.com/xiaoguai1818/sub2api>，用于临时工作分支、CI 和向精简版提交 PR，不维护独立产品版本。
- 目标：修复精简版实际存在的问题，更新已有核心能力，保持精简，不恢复已裁剪模块。
- 精简版与上游独立演进，双方都可能已有自己的修复。上游不是必须覆盖本地实现的标准答案。
- 本规范取代历史报告中“保留面新能力全部必须回移”等扩大范围的策略；历史记录保留用于追溯，不作为新增功能授权。

## 2. 回移边界

| 变更 | 处理 |
|---|---|
| 已有核心能力更新 | 评估后回移：网关转发、协议兼容、账号/渠道、用户/API Key、分组、鉴权、用量、必要管理、部署与安全 |
| 核心 bug、安全、兼容性、性能、可靠性问题 | 优先修复，并包含必要的前置变更和测试 |
| 仍在使用的外围功能 bug | 同样修复，保持最小改动 |
| 新平台、新模块、新入口、新用户流程或外围功能扩展 | 原则上不引入；不得因“有价值”而扩大范围 |
| 已裁剪功能 | 不恢复其代码、依赖、路由、页面、配置、数据模型或迁移 |
| 精简版已有等价修复 | 验证行为和测试后标记“已覆盖”，不重复回移 |

所谓新功能若实际仅补齐已有核心能力的必要兼容性，且不扩大产品边界，可按核心更新评估。按真实逻辑和问题链判断，不仅看 `fix/feat` 标题或文件名。

不恢复支付/订单/充值、卡密/兑换码/优惠码、邀请/推广/返佣、站点第三方 OAuth 登录/身份绑定、应用内在线更新/回滚，以及已确认裁剪的其他模块。公共修复与这些模块混杂时，只提取保留路径所需部分。

注意：站点 OAuth 登录不等于模型账号 OAuth 授权/令牌刷新；同名模块或不同版本也不能一概排除。以当前代码、实际调用路径和已确认裁剪边界判断，边界不明时交人工确认。

## 3. 独立修复与冲突

先检查精简版自己的提交、实现和测试，再判断上游修复是否仍需要。

- 精简版已完整解决同一问题：记录证据，跳过重复回移。
- 仅部分解决：评估缺失部分的最小补充，不破坏现有修复。
- 两种修复在行为、设计或实现上冲突（即使 Git 没有文本冲突）：暂停该项，列出双方方案，由人类决定保留精简版、采用上游、组合适配或暂缓。
- 文本冲突也须检查是否涉及独立修复；不能确认只是机械适配时，同样提交人工决定。
- 冲突清单写清：问题、双方 SHA/文件、行为差异、各自覆盖场景、风险、测试证据和建议。AI 可建议，不得自行择一覆盖或撤销精简版修复。
- 其他独立且已获确认的项目可继续；依赖冲突项的变更一起暂停。
- 已确认策略（2026-09-12）：数据库写入失败时仍保留账号内存冷却；不得因数据库或缓存快照未记录冷却而自动撤销，修改相关逻辑必须保留回归测试。

## 4. 执行步骤

### A. 核对基线、扫描与评估（先不改代码）

1. 检查工作区、远程地址及上一轮 PR；未合并、未放弃的 PR 和未提交成果先保留并说明。每轮以精简版最新提交为基线，同步个人仓库及本地工作目录；已合入精简版的旧成果不再独立保留。更新上游引用，浅克隆按需补齐历史。工作规范和待办记录须保留，不能随代码同步丢失。
2. 从历史报告核对上次已审查的精确上游 SHA，以及已实际回移的精简版提交。审查过不等于已合入，版本号相同不等于实现相同。
3. 默认扫描至最新稳定版并固定目标 SHA；主线未发布的紧急修复单列，不无限扩大窗口。以前未解决的候选继续保留。
4. 阅读实际差异及必要前置提交，与精简版当前代码和测试比较，检查路径存在性、独立修复、依赖、迁移、裁剪边界和风险。
5. 输出简短候选表：上游 SHA/PR、问题或核心更新、精简版影响、是否已覆盖、最小方案、依赖/风险、测试要求、建议。
6. 分类为：优先修复 / 核心更新 / 保留外围 bug / 已覆盖 / 不适用或新增功能排除 / 冲突待人工决定 / 暂缓。人工确认后实施。

### B. 最小回移

- 从确认的精简版基线建立独立 `backport/...` 分支，一个问题或强依赖问题链一组。
- 可直接回移时用 `git cherry-pick -x`；否则最小适配，并记录 `Upstream-reference: Wei-Shaw/sub2api@<SHA>`。
- 不全量 `merge upstream/main`，不为编译通过恢复裁剪模块，不做无关重构或格式化。
- 遇到独立修复冲突按第 3 节处理；不擅自硬解。
- 不覆盖未完成成果；个人中转仓库仅为重新对齐精简版基线时可重置，先核对差异并保留恢复引用，远程确需改写时用带精确预期 SHA 的 force-with-lease。不得强推精简版仓库。

### C. 验证、审查与记录

- 每轮先做本地验证，再在 GitHub 跑当前项目的 CI 和 Security Scan 全部检查；以最终提交为准记录 SHA、运行链接、结果及未运行原因，跳过或未运行不能称为通过。
- 当前 CI 包含部署脚本、后端 embed 编译、unit/integration 测试、前端 lint/类型检查/关键 Vitest、golangci-lint；Security Scan 包含 govulncheck 和前端依赖审计及例外校验。以当轮配置为准，根目录 make test 不能替代完整 CI。
- 必须覆盖相关回归测试与受影响后端包；修改前端时做类型检查、相关测试和构建。
- 涉及数据库时检查迁移编号与依赖，验证新库及旧库升级；不能直接照搬上游迁移编号。
- 检查裁剪功能未回流，并按环境验证鉴权、API Key、账号配置和网关请求等核心路径。
- 测试失败先查明原因；核心回归、迁移失败或数据风险不明时停止该项。未运行不能称为通过。
- 本地修改及验证完成后，汇报并提示用户确认推送；获准后推送个人仓库工作分支，运行并检查 GitHub CI 和 Security Scan，失败则修复重验。通过后从个人工作分支向 CherryLover/sub2api 的 main 提交 PR，交付链接和验证结果。
- AI 交付止于可审查的 PR，不自行合并、发版或推送镜像；个人 fork 的自动发版配置须在同步前核对，避免基线同步触发发布。精简版合入后本轮结束，旧工作分支和代码目录可清理，下轮重新从精简版开始。
- 更新版本窗口报告和回移台账，记录已回移、独立修复覆盖、排除、冲突、暂缓及未解决项，保留上游来源、精简版提交/PR、测试证据和回滚方式。

## 5. 历史基线与台账

历史报告：`docs/maintenance/upstream-reviews/2026-09-08-upstream-v0.2.3.md`。
后续报告：`docs/maintenance/upstream-reviews/YYYY-MM-DD-<版本>.md`。

已知核对事项（不是新增回移结论）：
- 已核对：v0.2.3 标签为 `8fa67d477d6651a744754392a8982ea589c26ae6`；旧报告目标 `772a0382f079676983c06f24b0d41e09139a8462` 是其后的 VERSION 同步提交。本轮进度见 `docs/maintenance/upstream-reviews/2026-09-12-upstream-v0.2.4.md`，已实施与待审查项分别记录。
- 原台账记录 #6122 / `a177b88e52` 的 DOMPurify 修复和其余 249 项评估；历史报告另列多组已实施回移，原台账进度不完整。
- 当前 package.json 已包含 DOMPurify `^3.4.14`；其他项须结合提交、代码和测试核实，不把旧报告中的“待回移”直接当成当前待办。
- 历史数量和旧分类仅作线索，不继承“新能力全部回移”规则。

每次报告至少保留以下字段：

| 上游范围/SHA/PR | 问题或核心更新 | 精简版现状与证据 | 决策/人工选择 | 精简版提交/PR | 验证及未解决事项 |
|---|---|---|---|---|---|

完成标准：需要的问题得到验证修复，已有核心能力得到必要更新，独立修复不被误覆盖，裁剪功能不回流，过程可追溯、可回滚。
